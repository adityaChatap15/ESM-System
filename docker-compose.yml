services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: esm_user
      POSTGRES_PASSWORD: esm_password
      POSTGRES_DB: esm_db
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
